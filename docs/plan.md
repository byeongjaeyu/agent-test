# 계획

> 이 문서가 프로젝트의 단일 기준이다. 작업을 시작하기 전에 항상 먼저 읽는다.

## 무엇을 만드는가

(한 문단으로. 누가 쓰고 무엇을 해결하는지.)

## 범위

**만든다**
-

**안 만든다**
-

## 단계

- [x] 1. GET /hello 에서 {"msg":"hi"} 를 반환하는 최소 FastAPI 앱 작성
- [x] 2. tests/ 에 /hello 엔드포인트 테스트 추가
- [x] 3. 기존 파이썬 테스트 삭제, Vite + React 로 로컬 상태만 쓰는 최소 todo 앱 작성 (frontend/)
- [x] 4. todo 상태관리를 App 내부 useState 에서 React Context(useReducer 기반 TodoContext)로 이전

완료한 항목은 `[x]` 로 바꾸고, 새로 알게 된 것은 이 문서에 반영한다.
